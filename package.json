{
  "name": "reverie-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rslib build",
    "typecheck": "tsc --noEmit",
    "pretest": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit",
    "run:history": "node skills/agentic-history-search/dist/history-search.cjs"
  },
  "devDependencies": {
    "@rslib/core": "^0.19.6",
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "zod": "^4.1.8"
  }
}
