{
  "name": "reverie-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rslib build && node scripts/postbuild.mjs",
    "typecheck": "tsc --noEmit",
    "pretest": "npm run build",
    "test": "rstest run",
    "test:watch": "rstest watch",
    "lint": "tsc --noEmit",
    "run:history": "node skills/agentic-history-search/dist/history-search.mjs"
  },
  "devDependencies": {
    "@rslib/core": "^0.19.6",
    "@rstest/adapter-rslib": "^0.2.0",
    "@rstest/core": "^0.8.5",
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "zod": "^4.1.8"
  }
}
